#include "io.hpp"

#include <filesystem>
#include <vector>

void writeGroups(const std::vector<Group>& groups, const std::filesystem::path& outputDir) {
    // TODO: implement
    (void)groups;
    (void)outputDir;
}
